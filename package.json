{
  "name": "paprika",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.4.0",
  "scripts": {
    "build": "bash scripts/build-web.sh",
    "dev": "bun run build && wrangler dev",
    "check:rust": "cargo fmt --all -- --check && cargo clippy --workspace --all-targets -- -D warnings && cargo test --workspace",
    "check": "bun run check:rust && bun run build && wrangler deploy --dry-run",
    "deploy": "bun run check && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "4.127.1"
  }
}
